name: Capture gold close backup

on:
  # GitHub scheduled jobs can start hours late, so this task cannot be trusted
  # as a 16:00 ET anchor. Keep it only for manual spot-feed diagnostics.
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: market-prices-writer
  cancel-in-progress: false

jobs:
  capture-gold-close-backup:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Refresh and commit backup sample
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          for attempt in 1 2 3; do
            git fetch origin main
            git switch --detach origin/main
            python -m py_compile scripts/update_market_prices.py
            python scripts/update_market_prices.py

            if git diff --quiet -- data/market-prices.json; then
              echo "No market price changes."
              exit 0
            fi

            git add data/market-prices.json
            git commit -m "Capture backup market close sample"
            if git push origin HEAD:main; then
              exit 0
            fi
            echo "Push attempt $attempt failed; refreshing from the latest main."
            sleep $((attempt * 5))
          done

          echo "Could not push backup market close sample after 3 attempts."
          exit 1
